# Fleet Vehicle Request Dashboard

This folder is ready for GitHub Pages. Upload `index.html` to a GitHub repository, then enable GitHub Pages from the repository settings.

Important: this is a static demo-style site. Data is stored in each user's browser using localStorage. It is not a shared database and the PINs in the JavaScript are not secure for protected information.
